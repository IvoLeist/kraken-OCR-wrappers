# Kraken OCR wrappers

Kraken OCR wrappers provides helper scripts, notebook glue, and sample outputs for generating segmentation comparisons.

## Segmentation diff

The rendered segmentation comparison is published on the page below and generated from the committed baseline and boxes outputs during the build.

<p><a href="segmentation-diff/">Open the segmentation diff page</a></p>

## What the site shows

- the generated segmentation comparison
- the scripts used to build it
- the sample outputs checked into the repository
